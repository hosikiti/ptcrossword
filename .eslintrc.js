module.exports = {
  root: true,
  extends: ['react-app'],
  plugins: ['react'],
  settings: {
    react: {
      version: 'detect',
    },
  },
  rules: {
    // Add custom rules here if needed
  },
}; 